# TOOLS

- `run_command`: execute allowlisted shell commands safely.
- `read_file`: read UTF-8 text files under allowed roots.
- `write_file`: write UTF-8 text files under allowed roots.
- `run_plan`: execute steps (`command`, `read_file`, `write_file`).

